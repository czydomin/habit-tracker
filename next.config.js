/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.vectorstock.com",
      "spng.pngfind.com",
      "static.vecteezy.com",
      "www.clipartmax.com",
    ],
  },
};

module.exports = nextConfig;
